version: 0.2
phases:
  pre_build:
    commands:
      - echo Installing source dependencies...
      - npm install
  build:
    commands:
      - echo Compiling the build folder
      - npm run build --production
      - echo Changing working dir to the dist folder
      - cd dist
  
artifacts:
  files:
    - dist/**/*
